{% set name = "napari-deeplabcut" %}
{% set version = "0.2.1.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/napari_deeplabcut-{{ version }}.tar.gz
  sha256: f995596e660d198bf8ca76bfb97f02e79ba16d81dfd9bccc05941a52cc6825ae

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - python >=3.8
    - setuptools-scm
  run:
    - dask-core
    - dask-image
    - napari <0.5.0
    - natsort
    - numpy
    - opencv
    - pandas
    - python >=3.8
    - qtpy
    - scikit-image
    - pytables
    - pyyaml

test:
  imports:
    - napari_deeplabcut
  commands:
    - pip list
    # - pip check  # Can be uncommented when upstream requirements are updated/corrected
  requires:
    - pip

about:
  home: https://github.com/DeepLabCut/napari-deeplabcut
  summary: napari + DeepLabCut annotation tool
  license: BSD-3-Clause
  license_file: LICENSE

extra:
  recipe-maintainers:
    - jeylau
    - goanpeca
    - jaimergp
